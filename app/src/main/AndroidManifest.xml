<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhuandian.androidstudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LinearLayoutActivity" />
        <activity android:name=".activity.GridLayoutActivity" />
        <activity android:name=".activity.FixLayoutActivity" />
        <activity android:name=".activity.ScrollFixLayoutActivity" />
        <activity android:name=".activity.FloatLayoutActivity" />
        <activity android:name=".activity.ColumnLayoutActivity" />
        <activity android:name=".activity.SingleLayoutActivity" />
        <activity android:name=".activity.OnePlusNLayoutActivity"></activity>
    </application>

</manifest>